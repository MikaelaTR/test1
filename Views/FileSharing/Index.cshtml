@model IEnumerable<AdvancedProjectMVC.Models.SharedFile>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "File Sharing";
}

<h1> Shared Files</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                File Name
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.FileName
                </td>
                <td>
                    <!--<a asp-action="DeleteFile" name="fileName" value="@item.FileName" >Delete</a>-->
                    <form method="get" asp-controller="FileSharing" asp-action="DownloadFile">
                        <input type="hidden" name="fileName" value="@item.FileName">
                        <input class="btn btn-success" type="submit" value="Download" />
                    </form>
                </td>
                <td>
                    <form method="post" asp-controller="FileSharing" asp-action="DeleteFile">
                        <input type="hidden" name="fileName" value="@item.FileName">
                        <input class="btn btn-success" type="submit" value="Delete" />
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<p>
    <a asp-action="Upload"> Upload New File</a> 
</p>