@model IEnumerable<AdvancedProjectMVC.Models.Student>

@{
    ViewData["Title"] = "Student List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="header">
    <i class="fa fa-group"></i>
    <h1 class="d-inline-flex">Student List</h1>
</div>

<p>
    <a asp-action="Create" class="btn btn-primary">Add New Student</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                Student Number @*@Html.DisplayNameFor(model => model.StudentNumber)*@
            </th>
            <th>
                First Name @*@Html.DisplayNameFor(model => model.FirstName)*@
            </th>
            <th>
                Last Name @*@Html.DisplayNameFor(model => model.LastName)*@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.StudentNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
@*                <td>
                    <a asp-action="Edit" asp-route-id="@item.StudentNumber">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.StudentNumber">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.StudentNumber">Delete</a>
                </td>
   *@         </tr>
        }
    </tbody>
</table>

