@model AdvancedProjectMVC.Models.Enrollment
@inject AdvancedProjectMVC.Services.OptionsService Options

@{
    ViewData["Title"] = "Enroll";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4>Course Enrollment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="Enrollments" asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                @*<select asp-for="CourseId" name="CourseId" class="form-control" asp-items="@(IEnumerable<SelectListItem>)ViewData["Courses"]"></select>*@
                @*@Html.DropDownList("CourseId", Options.GetCourses().Select(c => new SelectListItem() {Text = c.Title, Value = c.Id.ToString()}),"Choose Course")*@
            <div class="form-group">
                <label asp-for="CourseId" class="control-label">Select a Course:</label>
                <select asp-for="CourseId" class ="form-control form-select" asp-items="ViewBag.CourseId"></select>
            </div>
@*            <div class="form-group">
                <label asp-for="ApplicationUserId" class="control-label"></label>
                <select asp-for="ApplicationUserId" class ="form-control" asp-items="ViewBag.ApplicationUserId"></select>
            </div>*@
            <input type="hidden" asp-for="Grade" />
            <p></p>
            <div class="form-group">
                <input type="submit" value="Enroll" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
