@model IEnumerable<AdvancedProjectMVC.Models.ServerInvite>

@{
    ViewData["Title"] = "Server Invites";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Server Invites</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Date
            </th>
            <th>
                Server Name
            </th>
            <th>
                Sender
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateSent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Server.ServerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SenderUserName)
            </td>
            <td>
                <form asp-action="Join">
                    <input type="hidden" name="serverId" value=@item.serverId />
                    <input type="hidden" name="inviteId" value=@item.Id />
                    <input type="submit" value="Join" class="btn btn-success" />
                </form>
@*                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
*@            </td>
        </tr>
}
    </tbody>
</table>
